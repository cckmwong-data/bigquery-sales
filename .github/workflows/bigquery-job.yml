name: Run Python Script with BigQuery

on:
  schedule:
    - cron: '0 21 * * *'  # every day at 2200 UTC
  workflow_dispatch:  # allows manual trigger

jobs:
  run-python:
    runs-on: ubuntu-latest
    env:
      RUN_GENAI: "false"      # Gen-AI disabled in daily run
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Authenticate with Google Cloud
        env:
          GCP_CREDS: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}
        run: |
          echo "$GCP_CREDS" > gcloud-key.json
          gcloud auth activate-service-account --key-file=gcloud-key.json
          gcloud config set project new-project-456705
          export GOOGLE_APPLICATION_CREDENTIALS=gcloud-key.json

      - name: Run Python Script
        run: python Customer Segmentation_ai.py
